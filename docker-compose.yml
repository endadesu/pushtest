services:
  postgres:
    image: postgres:16
    environment:
      POSTGRES_DB: memapp
      POSTGRES_USER: memapp
      POSTGRES_PASSWORD: memapp
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
